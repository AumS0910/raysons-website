node_modules/
dist/
.DS_Store

# generated by build-deploy.mjs — the upload payload, rebuilt on demand
deploy/

.vercel

# Raw camera and generated masters. These are the sources we encode the web
# clips FROM — keep them on disk, but they must never enter git and must never
# be uploaded to the web server. "fd 3 Casting Video.mov" is the 467 MB master
# behind valve/about-film.mp4 on About Us.
*.mov
raysons-scrollyteller/valve/Monument_*.mp4
raysons-scrollyteller/**/Building_*.mp4
raysons-scrollyteller/**/*-src.mp4

# dev scratch — test harnesses, headless screenshots, server logs
_*.mjs
_*.cjs
_*.png
*.log
